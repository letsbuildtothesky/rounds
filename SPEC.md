# Rounds Specification Pointer

This file is **not** a product specification and must never become a second canon.

Implementation authority lives in the repository's current implementation baseline:

1. `specs/00-READ-FIRST.md`
2. `specs/product/`
3. `specs/engineering/`
4. `specs/build/` for the active implementation slice
5. `ux/` for current visual references

Historical specifications are not implementation sources unless a human explicitly cites one for investigation.

If current sources conflict, stop and report the conflict. Do not resolve it by searching old versions.
