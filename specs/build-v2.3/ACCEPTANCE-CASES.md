# Failure-detecting cross-system acceptance cases

Version 0.21 · 2026-09-09 · All cases below are NOT EXECUTED against the complete v2.3 application. They refine, not replace, the197 source cases in generated/acceptance.json.

## Changelog

- 0.21 (2026-09-09): ADR-M03 implements the Supabase private object adapter, four-minute signed-PUT gateway and default-off runtime connection. Local protocol/restricted-SQL tests are distinct from configured cloud/RLS, phone/UI and release acceptance.

- 0.20 (2026-09-09): ADR-S07 connects the native single-photo reservation/upload/verification and proof/completion chain. Preserve original identity, local files and designs; configured provider, signatures/multiple candidates/branching, offline/UI/mobile activation remain open.

- 0.19 (2026-09-09): ADR-S06 connects immutable observations to an encrypted arrival/pickup/handoff queue, exact server-compatible bytes/hash, original-fence predecessor bindings and foreground POST/status recovery. Photo uploader/proof/completion materialization, branched chains, stale-retention, full UI/offline and phone activation remain open.

- 0.18 (2026-09-09): ADR-T06 implements restricted SubmitProof/CompleteDelivery, exact note/location persistence, immutable pending candidates, whole-order completion and conditional Round/commitment closure. Add isolated migration0005 and replay/rollback/ownership/upgrade tests. Provider/native sender/phone/UI activation remain open.

- 0.17: ADR-M02/QA-DR-07 adds exact byte decoding, immutable handoff scope, fresh capability/replay, sealed-copy and IO-revocation/rollback tests. Configured provider/native uploader and full proof/completion acceptance remain unexecuted.
- 0.16: ADR-T05/QA-DR-05 local acceptance now uses real arrival endpoints and predecessor versions; add GPS/route-policy/current-stop/attempt/time/rollback negatives. Complete phone, provider/geofence, proof and stale-retention acceptance remain unexecuted.
- 0.15: add ADR-T04/QA-DR-07 handoff subcases through actual registration/pickup/HTTP/SQL; arrived state is explicitly seeded. Full phone arrival, verified proof/completion and stale-retention remain unexecuted.
- 0.14: add ADR-S05 camera-intent/foreground-return subcases with real encrypted storage and mocked camera/Auth/vault. Retake, original assignment/account/epoch, bounds, unresolved restart and lost acknowledgement are local evidence, not OS/UI/offline submission acceptance.
- 0.13: add ADR-Q02/QA-SYSTEM-QUERY-CLOSURE subcases: registered execution query drives real pickup; native response validation drives encrypted photo observations; denied scope, first-snapshot preservation, reassignment and late responses. Full phone/provider/UI and J04 remain unexecuted.
- 0.12: add AC-05 immutable observation/photo transaction and recovery subcases. Trusted context fixture validation is not authenticated work-query authority, camera integration or full J04.
- 0.11: add actual cold-start entry-point and DriverApi Auth race/expiry subcases. Default-off diagnostic wiring is not phone/OS, mapped migration, camera/offline or full J04 acceptance.
- 0.10: add AC-05 lifecycle composition, failed-account-change and native-root/backup packaging subcases. Actual app event/camera/legacy-writer integration, OS backup/restore and full J04 remain open.
- 0.9: add AC-05 legacy encrypted preservation subcases: exact bytes/drafts/offsets, interrupted copies, source changes, missing/unsafe/orphan files, key/DB loss, corruption and writer exclusion. No account mapping, mobile/broker or full J04 acceptance.
- 0.8: implement AC-05 local encrypted-byte/restart/key-loss/account-lock subcases and real SQLCipher process-kill rollback; mobile encryption/backup, legacy conversion and full J04 remain unexecuted.
- 0.7: extend AC-05 with native credential failure/revocation and read-only Driver5 preservation subcases; full encrypted conversion and device acceptance remain open.
- 0.6: add actual enrollment/renewal/revocation SQL/HTTP tests and registration-issued pickup/recovery evidence. Native/provider and full cross-system gates remain open.
- 0.5: add actual HTTP/device/principal/status SQL evidence for AC-01/03/05/07 subsets; provider responses and device issuance remain fixture-controlled, with full cross-system cases still NOT_EXECUTED.
- 0.4: link real local-pickup custody/attempt/batch/replay/rollback and wire tests; retain full cross-system NOT_EXECUTED status.
- 0.3: link full PostGIS whole-order and actual-row transaction tests without promoting cross-system cases to passed.
- 0.2: link the real PostgreSQL transaction subset for AC-01/03/05/07 without promoting full application cases to passed. Receipt isolation includes the same actor across tenants and cities, raw SQL and current-authority revocation.
- 0.1: define cross-system failure-detecting scenarios.

AC-01/04/05/07 handoff subcases are in [restricted handoff tests](../../services/api/test/v23/handoff-http.postgis.ts): actual registration→pickup→dropoff arrival→handoff→replay/status. Only pickup-arrival setup is still a fixture in this older suite. Assert one custody debit, stable IDs, exact decimal/multi-line quantities, linked recipient/alternate policy, authorised unattended location, missing/future/unsupported policy rejection, unchanged delivery outcome/evidence/claim, no proof/assets/customer effect, original scope/device revocation and full SQL/result-failure rollback. Provider Auth is mocked. Verified upload/proof/completion, stale retained_for_review and installed-phone acceptance remain open.

AC-01/05/07 arrival subcases are in [restricted arrival tests](../../services/api/test/v23/arrival-http.postgis.ts): real registration→work query→pickup arrival→whole pickup→dropoff arrival→handoff. Use query IDs and own predecessor versions; record exact GPS/accuracy or null-point policy override, never a fabricated geofence/receipt/readiness/custody. Test shared-pickup policy across every order, missing/future/schema-invalid policy, original assignment/actor/device/city, current-stop and actual attempt ownership, causal timestamps, replay/races and SQL/result rollback of observation/stop/attempt/fact/outbox. Auth/GPS are synthetic and schema/role execution is real; phone/UI, actual provider geography, stale retention and final completion are not certified.

AC-01/04/05/07 POD subcases are in [asset tests](../../services/api/test/v23/pod-assets.postgis.ts): actual registered arrival/pickup/handoff handlers, immutable purpose/owner/assignment binding, fresh capability/status recovery without stored secrets, real JPEG/PNG byte/hash/size/full-decode checks, sealed-copy reread and old staging-token isolation. Wrong actor/tenant/city/purpose, device revocation or assignment change during IO, corrupted/missing object, signer/seal outage, rollback and races cannot create a verified fact. Receiver correction blocks signature while preserving a valid photo. Old unbound assets remain unchanged. Auth/Storage are labelled doubles; configured cloud/RLS, scanning/viewer, full UI and J04 remain unexecuted. ADR-M03 extends these tests through the actual Supabase REST adapter/gateway (upstream remains a double), verified photo, proof and completion without IO under SQL locks or capabilities in receipts. [Provider tests](../../services/api/test/v23/supabase-pod-storage.test.ts) cover private configuration, bounded bodies/streams, wrong hash/type/header/audience, expired/altered grant, no overwrite, uncertain acknowledgments, cancellation/redirects and local Node ingress.

ADR-T06 adds actual SubmitProof/CompleteDelivery tests to the same suite: six proof kinds, exact notes/arrival references, incomplete immutable candidates, invalidated/foreign proof, original scope denial, receiver correction, rollback, replay after closure, races, open duties and multi-order query continuity. Scoped commitment privileges deny foreign owner/city/tenant access and reservation creation/range edits. [Upgrade test](../../services/api/test/v23/proof-upgrade.postgis.ts) preserves old proof rows through nullable migration0005, not Driver5 conversion. Full phone/provider/UI and J04 remain unexecuted.

AC-05 ADR-S06/S07: [queue tests](../../apps/driver_harness/test/v23_offline_command_test.dart) and [photo-transfer tests](../../apps/driver_harness/test/v23_photo_transfer_test.dart) cover original-fence canonical commands; unit/handoff/location/asset/proof binding; reservation SQL rollback; six persist/upload/verification interruption boundaries; ephemeral capability renewal; same-ID lost-PUT verification/status/reupload recovery; denied hosts/tokens/methods/redirects; sign-out during upload; and untouched local photos. Production TypeScript validates eight native requests/hashes, with HTTP/Auth/vault doubles. Configured provider, mobile kill, signatures/multiple candidates/branches and full J04 are NOT_RUN.

AC-04/05 camera hand-off subcases are in [camera capture tests](../../apps/driver_harness/test/v23_camera_capture_test.dart): persist intent before launch, background lock then same-account foreground reauthentication, early plugin completion, concurrent same-bearer refresh, failed/successful retake and cancellation preserving prior bytes, account/Auth/epoch denial, invalid/oversized/growing source rejection, original-fence retention during reassignment, exact-byte saved-ack retry and unresolved intent after store restart. Camera/Auth/vault are doubles; SQLCipher/files are real. No automatic lost-data adoption, local proof-to-completion shortcut, OS camera kill/source-cache protection, full screen routing or J04 acceptance is claimed.

The [transaction checkpoint](W01-TRANSACTION-CHECKPOINT.md) maps the five-table foundation, complete-schema guards and [actual local pickup](../../services/api/test/v23/confirm-local-pickup.postgis.ts). The latter tests AC-01/03/05/07 subsets with source-derived wire validation, whole-batch rejection, exact physical ledger/attempt writes, rollback, replay, original assignment and revoked scope. [Execution query tests](../../services/api/test/v23/driver-execution.postgis.ts) construct pickup from actual authorized query output and exercise wrong tenant/city/job/device and missing policy. [Native query tests](../../apps/driver_harness/test/v23_execution_query_test.dart) exercise original context/photo retention, revoked/late responses, revision conflicts and generated finite response validation. Inbound markers cannot fabricate receipts; prior stock cannot be reintroduced. These are lower-level evidence, not the complete HTTP/custody/offline/permissions/UI workflow.

The [HTTP/PostGIS tests](../../services/api/test/v23/pickup-http.postgis.ts) exercise actual localhost transport, principal mapping, device revocation/epoch, membership/city/tenant isolation, original-job result lookup/replay and malformed/oversized requests. [Auth/device unit tests](../../services/api/test/v23/device-session.test.ts) exercise signed capabilities and the Supabase client using mock provider responses. The [registration suite](../../services/api/test/v23/device-registration.postgis.ts) additionally obtains its capability from actual server enrollment, then runs pickup/status/revoke; tests duplicate races, lost commit acknowledgement, atomic security audit, account isolation and rate admission. These do not certify configured Auth, native enrollment/storage, offline migration, device UI or production ingress.

AC-05 encrypted-storage subcases are mapped to [real file/SQLCipher tests](../../apps/driver_harness/test/v23_encrypted_work_store_test.dart) and [actual process-kill verification](../../tools/verify_v23_sqlcipher_crash.py): wrong/no key cannot read bytes; file modification/truncation/substitution/purpose tamper fails; four staged-save interruption points never fake server completion; key/schema/database loss cannot reset evidence; account switch/revoke locks old handles and late results; orphans remain retained; a second process cannot acquire the writer; SIGKILL with an encrypted hot journal rolls back uncommitted rows. Run these alongside the actual schema5 preservation/outbox tests. Local macOS byte tests, mocked vault/Auth, injected file interruptions and unexecuted mobile/backup/legacy conversion are distinct evidence classes; full AC-05/J04 remains open.

AC-05 legacy preservation subcases are in [archive tests](../../apps/driver_harness/test/v23_legacy_recovery_archive_test.dart): actual schema5 DB image and media bytes round-trip through encrypted chunks; raw drafts, original IDs/JSON/offsets are unchanged; missing/unsafe references never read outside owned media roots; interruptions at manifest/chunk/file/recheck/receipt resume without duplicating receipts; changed sources cannot seal a mixed copy; absent/corrupt keys/DB and corrupt archived content fail closed. This archive stays unassigned and offers no account content/export/replay interface. Vault and preferences are test doubles; actual paused-writer/mobile kill/backup and original-owner/server reconciliation remain open. The separate existing SQLCipher SIGKILL test does not certify a process kill during this converter.

Use isolated fixtures with two tenants sharing display names, two cities, authorised/revoked/wrong-role actors, real stable IDs and controllable server clock/network/provider fakes. Include current source hash, actual commit, seed identity and expected exact row/event counts. Fakes must be labelled and cannot certify a real provider. Each case also needs corresponding visible state assertions and negative actor. Test code belongs under `services/api/test/v23`, database integration tests and Driver/Operations integration/golden suites when implemented.

AC-05 lifecycle composition uses [lifecycle tests](../../apps/driver_harness/test/v23_storage_lifecycle_test.dart): no account enrollment before preparation; failed/rootless startup stays locked; actual closed Driver5 archive precedes opening an unrelated fresh account store; sign-out and failed account switch invalidate old leases; interrupted staged capture retains the original photo; Flutter background/resume never automatically authenticates; explicit revoke retains files; disposal invalidates pending startup; another runtime owner cannot prepare/enroll. Auth/vault remain mocked, root responses are mocked separately from Java/compiled-APK checks, and old controller writer coordination/camera/offline resume/OS restore remain NOT_RUN.

AC-05 startup/Auth subcases use [startup tests](../../apps/driver_harness/test/v23_startup_test.dart) and [actual DriverApi tests](../../apps/driver_harness/test/v23_driver_auth_test.dart): controller/DB construction is denied during preparation; failed/late preparation never starts legacy workers; default-off entry uses no native services; real closed schema5/raw drafts survive preservation. OTP/refresh responses and native token writes delayed across sign-out cannot revive tokens or storage; failed B cannot poll back into A. Verify current bearer before queue opening, HTTP401/403 lock, same-token coalescing, conservative expiry and real encrypted-handle denial. Auth/vault/path response doubles and actual macOS SQLCipher are explicitly distinct from configured Auth, phone cold-start, camera/offline continuation and mapped migration. No full J04 PASS follows from these subcases.

AC-05 observation subcases are implemented in [observation persistence tests](../../apps/driver_harness/test/v23_offline_observation_test.dart): freeze original assignment/policy/device/epoch; refuse unknown contexts, foreign entities, conflicting IDs and wrong/future/missing parents; atomically roll back observation and photo intent on SQL failure; recover five save-interruption points without false saved/verified status; reject tampered/missing photos and cross-account access; keep exact old intent after assignment refresh. Fixture contexts are trusted internal test inputs, not an authenticated work projection. Actual camera, command bindings/materialization/sending, legacy mapping and full J04 remain open.

| ID / release | Setup and actions | Failure-detecting assertions |
| --- | --- | --- |
| AC-01 R1 · J01 | Commit one ready whole delivery, plan/stage/release, pickup, arrival/handoff, reserve/upload/verify/submit proof, complete. Replay each same command after dropped response. | Exactly one delivery/unit/physical attempt/handoff/proof chain; custody balances correct; delivery.completed and round.completed once on actual branch; command receipts return original IDs; both Driver and History show same outcome. |
| AC-02 R1 · slot race | Capacity1, two drafts request concurrently; move winner to full target, reduce occupied cap, expire/refresh old hold with controlled clock. | One winner; loser SLOT_FULL; failed move preserves original hold; reduction retains commitment and flags overcommit; expired ID never revived; template revision doesn't create fresh capacity. |
| AC-03 R1 · J03 whole order | D1 five required/three ready, D2 complete on proposed route. Submit short/extra/duplicate/missing-line pickup; call disabled approval as driver/operator/owner. Replan D2 explicitly then collect it. Later prepare allfive and collect D1. | Failed batch has zero custody/attempt/success facts, D2 not silently collected; authorised approval always FEATURE_NOT_ENABLED and no split; D2 proceeds only after explicit adjustment; D1 one whole unit; direct second unit/child insert fails actual SQL constraint. |
| AC-04 R1 · pending proof | Valid physical handoff and local photo, cut connection midupload, change receiver, fail replacement capture, then resume/verify. Attempt completion with unverified/wrong-purpose/wrong-handoff asset. | Pending UI never says server completed; original photo preserved, prior receiver signature invalidated only as applicable; no delivery.completed before valid proof; wrong assets rejected with no completion/custody changes. |
| AC-05 R1 · J04 offline | Cache assignment7, disconnect before pickup, observe pickup→arrival→handoff→proof→complete; kill/restart at each boundary. Lose server pickup response; shuffle result attempts. In second run reassign toversion8 before reconnect. Also run native identity retry/revocation/storage-failure tests and read-only schema5 preflight with pending/unknown/uploading rows and files. | Frozen original ID/hash/time/fence survive; GetCommandStatus recovers predecessor IDs; binds correct unit not array index; one successful chain. Reassigned chain retained for review with incident, zero current-assignment mutation and no success bindings. Other principal cannot read/sync local bytes. Preflight changes no source DB/media bytes, refuses unknown schema and never authorizes automatic replay for unproven owners/fences. Mock credentials and SQLite preflight do not pass native encryption/conversion. |
| AC-06 R1/R2 · return/transfer | Pickup then cancel; initiate transfer, wrong receiver responds, actual receiver receives2 then3 of5; replay and try another1; attempt decline after first receipt. | Cancellation cannot erase in-custody balance; wrong actor denied; source debited/receiver credited exactly actual amounts; replay adds0/excess rejects; postreceipt decline disallowed; closure only after explicit disposition; no residual delivery automatically created. |
| AC-07 all · privacy | Two tenants/cities/same driver-name fixtures; guess IDs, switch pooled context, revoke active grant/membership, request media/history/export and live GPS, then sign in another Driver principal. | No cross-scope rows/URLs/stream content; role cannot set arbitrary trusted context; exact GPS stops after revocation incl active subscription; refetch denied; pending old evidence retained but unreadable to new principal. |
| AC-08 R2 · communication/effects | Stage voice/file in threadA, switch toB/city, receiveA reply, send with response loss; edit template between provider timeout and retry; crash worker after provider accepted. | Draft remainsA and sends once toA; unread/tray consistent, no focus theft; stable message/effect IDs; template frozen; reconcile unknown provider effect before retry, no duplicate external message; no fake read/connected state. |
| AC-09 R1/R2 · source and draft | Same signed provider event twice then changed payload; late AI suggestion on edited draft; older source revision after pickup; batch with valid/invalid rows. | One durable source receipt/delivery; changed hash rejected/quarantined; no late overwrite; loaded manifest immutable; precise per-row results; no resend to customers from fixtures. |
| AC-10 R2 · protected plans | Overnight shifts, cargo classes including max0 and tied limiting dimensions; stop move by pointer and keyboard; delay staged inbound, supersede change, refuse freelance amendment. | Same feasible sequence/result across input methods; hard limit blocks; downstream return/reload/free-after recalculated; stable stop IDs; old ack doesn't acknowledge newer instruction; refused amendment leaves original agreement; no auto broadcast. |
| AC-11 R0/R2 · maps/device | Change style/2D/3D/rotate/North/focus/back, stale/late GPS, failed weather/traffic, incoming call/lock/battery saver/navigation+telemetry. | Geographic anchors stable; fresh current position never rewinds; approved panels/attribution intact; no fake rain/traffic estimate, justified buffer only; actual device report for allthree classes with measured freshness/battery and correct failure classification. |
| AC-12 R2 · tracking/history | Compare board/history/export on same cutoff incl pending proof and return; buyer/recipient surprise tokens; revoke grant during export. | Same defined counts/denominators; pending proof excluded from fully evidenced totals; recipient cannot see buyer/private data or other route; revoked download denied; CSV formulas escaped and no unrelated signed links. |
| AC-13 R3 · J02 acceptance | Own driver free; operator broadcasts to two eligible open freelancers, concurrent acceptance, one globally conflicting job, drop winner response; goods unready. | Broadcast allowed without capacity prerequisite; exactly one agreement/assignment/released Round/commitment/settlement; loser no private details; retry returns winner IDs; job visible immediately but pickup blocked until actual ready. |
| AC-14 R3 · J05 settlement | Guarantee1000; recordpayment400; complete scope; record600; authorised adjustment−100; refund100. Duplicate references, excessive refund and finance-only evidence decision. | Earned/paid/due/refund dimensions match each step; replay adds0; excessive refund denied; valid proof unchanged by finance; rejected proof needs genuine authorised replacement; no bank payment executed. |
| AC-15 R4 · J06 receipt | Stage R1(D1–3),R2(D4–5) before09:00 truck; delay to10:00; pause workers; receive1,2,4,5 then3; wrong line/receiver/concurrent release. | Same route/stop IDs; real receipt only; R2 ready/releasable independently while R1 showsD3 blocker; gates current without worker; laterD3 no duplicate delivery; exact balance/role checks; local-only tenant unaffected. |
| AC-16 all · UI parity | Approved source and actual app same state/data/viewport. Capture all sheets/errors/retry and run touch/keyboard/back/rotation/large-text. | Named source/hash and screenshot pairs/diff; no unexplained app-owned geometry/type mismatch; every visible control has real state/command or explicit local-only purpose; no hidden dead end or prototype success button; reference missing state recorded, not improvised. |
| AC-17 release · restore | Backup database+private media; create uncertain provider effect and pending offline client; restore isolated environment, replay only permitted intents. | Referential assets/command/effect receipts preserved; no duplicate send/writeback/payment record; held/expired GPS purge correct; actual RPO/RTO measured; old client compatibility or explicit upgrade/recovery; no silent evidence loss. |

For each of197 source cases, keep its ID and exact expected behaviour, attach implementing test/state/trace, and strengthen generic “setup/execute/check” prose into concrete fixtures/assertions before marking it runnable. Passing these17 cases alone is not complete coverage. Critical unresolved semantics in AUDIT block their affected test signoff and implementation, not honest documentation of the rest of the product.
