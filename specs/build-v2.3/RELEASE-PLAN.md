# Release implementation sequence

Version 1.6 · 2026-09-08 · Active implementation sequence; release activation remains gated.

## Changelog

- 1.6: native credential and fresh encrypted-store seams now have local evidence; mobile/provider verification and actual legacy conversion still gate activation.
- 1.5: server-side device enrollment/refresh/revocation reaches actual pickup/status in isolated tests; native persistence, configured Auth and old-client upgrade remain activation gates.
- 1.4: isolated authenticated pickup/status HTTP tests now run; configured Auth, device enrollment, legacy migration and endpoint/client activation remain gated.
- 1.3: first-local pickup now has real restricted-role mutations and wire validation; no endpoint/client activation.
- 1.2: full fresh schema and targeted transaction tests implemented; actual-handler/client wiring remains gated.
- 1.1: record the implemented W01.2 transaction foundation without promoting full-schema/handler/client or release gates.
- 1.0: close routine reconciliation, authorize scoped ready work from existing code, isolate business gates and choose W01.

## Current implementation

Native follow-through: [storage checkpoint](W01-NATIVE-STORAGE-CHECKPOINT.md) distinguishes implemented credentials and fresh SQLCipher/AES-GCM byte/process-kill tests from pending mobile vault/backup/legacy conversion and phone activation. This does not promote R0/R1 acceptance.

The user authorizes implementing [ready module scopes](READINESS.md). [W01](FIRST-WORKFLOW.md) is first in an existing own-team workspace. W01.1 has the exact whole-order gate; W01.2 has the pinned runner, complete isolated PostGIS schema, actual first-local-pickup transaction and gated authenticated pickup/status plus device registration/refresh/revocation transport. Broader custody variants, configured Auth/mobile storage verification and client migration/wiring remain unfinished; see the [checkpoint](W01-TRANSACTION-CHECKPOINT.md). New tests issue device credentials through actual registration code; upstream Auth responses remain mocked. DEC-01 gates only new workspace admission. Unapproved configuration stays disabled; fixtures are explicitly synthetic.

Verify current Git/db/client versions; preserve development data and pending evidence. No reset, force-push, broad SQL import or old-client reinstallation that erases queues. Shared environment changes and release activation require explicit scope approval. Ready means settled requirements for the named scope, not code complete or tests passed.

## R0 — foundations and field evidence

1. BS-00/01/13: isolated development DB/roles, generated contract compile, two tenants, explicit city/capability tests and stable command runner. Apply reference SQL only in the declared fresh environment.
2. BS-12: establish approved reference capture conditions and shared Driver/Dispatch components; no app-wide redesign.
3. BS-05/08: verify the combined navigation/location/evidence harness and encryption choice. Existing successful Android observations are useful leads; they do not pass new iOS/OEM/offline gates.
4. Exit: restricted-role schema tests, ID/hash replay, generated clients, secret separation and R0 field report. Missing hardware may keep field gate open while approved server work proceeds; never relabel it PASS.

## R1 — complete English own-team loop

Dependency order (each checkpoint includes applicable design and negative tests):

1. Workspace setup/auth and truthful permissions → city/site/proof policy → manual draft/address/slot → committed whole delivery (BS-01/02).
2. Minimal deterministic plan/proposal/apply/stage/release, team shift/vehicle and acknowledged assignment (BS-03/06). Full optimisation is not necessary for the first end-to-end path.
3. Pickup arrival → exact full manifest collection → navigation → handoff → local capture → verified proof → completion/history (BS-04/05/08/09). J01/J03/J04 and AC-01–06 are mandatory.
4. Basic Operations↔Driver messages, issue/hold and return/disposition allow the loop to recover, not just succeed (BS-04/07).
5. Pilot source normalisation/writeback through the same canonical intake (BS-09), with adapter fixture then verified provider connection; manual entry remains usable.
6. Exit: English visual comparisons for all involved states, real-device full path/restart/response-loss, isolation and recovery evidence. No invented whole-product completion percentage.

## R2 — full English own-fleet depth

Complete planning/timeline/capacity/overnight and live-change controls; all applicable settings, schedules, relationships, media/voice-note/comms/notifications, history metrics, exports, sources/customer tracking and return/recovery. Map controls, weather provenance and entrance inspection follow BS-08/12; unresolved providers have real setup/unavailable states. Close every R2 feature row and its explicit UI state binding. Integrated VoIP is separately gated, not simulated by a timer. Test full Dispatch laptop/iPad matrix and Driver small/large-text states.

## R3 — freelance product

BS-06/10 plus shared modules: global driver onboarding/verification, voluntary availability, explicit operator broadcasts, one-winner acceptance into actual executable work, changes/consent, direct settlement records and typed disputes. Full English C/B-Network/K flows, cross-tenant privacy and provider decisions are required. No own-capacity prerequisite, percentage commission, wallet or payment execution. Approval of R1/R2 is not permission to enable R3.

## R4 — optional Intercity

BS-11: trip/load/receiver authority, pre-arrival local plans, independent readiness, delay/reallocation and full custody chain. New transport/receiver tasks beyond47 files need design approval before implementation. Local-only tenants never depend on this module.

## Localization and finish

Keep IDs/state logic language-neutral and existing user language preference/data intact. New Thai visual implementation waits until the English scopes under review have passed functional and visual acceptance, per user direction. Before claiming the full specified product complete, cover English and Thai across all promoted releases and required native/provider/security gates. Deferred features remain explicitly excluded, never silently forgotten.

Each checkpoint report lists exact source/commit, features, screens/states, tests run, remaining decisions and next bounded task. No automatic promotion merely because all files in a folder exist.
