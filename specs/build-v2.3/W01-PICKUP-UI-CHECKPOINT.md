# W01 — first visible English pickup port

Version 1.11 · 2026-09-10 · Staged UI/data/confirmation/issue-form implementation, not end-to-end v2.3 acceptance.

## Changelog

- 1.11 (2026-09-10): mount the same authorized report/reply workflow in the Phase39 shell at default-off `/dispatch`, with local search, responsive list/detail placement and tablet switching. Preserve original drafts, legacy root and phone; general Board/map data and configured end-to-end acceptance remain open.
- 1.10 (2026-09-10): add authorized opt-in current labels to the source-derived Dispatch delivery cards/detail, nullable missing/ambiguous metadata and old-wire compatibility. Use the source's final queue overrides with a direct regression check, not its earlier base card styling. Verify restricted-role isolation/original assignment/draft preservation; fix border-box sizing in the component and synthetic test wrapper. Main-board/configured-host/phone activation remain separate.
- 1.9 (2026-09-10): implement the bounded Dispatch drawer/form, verified-login host and default-off route. Actual browser component checks caught native-fetch binding and focus/copy defects; production build caught TS import resolution. Record user-supplied source screenshots, exact configuration and remaining original-source/Auth/phone/full-board gates.
- 1.8 (2026-09-10): add tab/login-scoped draft and exact-attempt reload recovery, persist-before-POST and failed-settlement/competing-writer checks. Record bounded drawer approval; original source render, mounted host and actual browser/phone acceptance remain open.
- 1.7 (2026-09-10): implement finite Operations issue discovery and static-schema browser reply transport with original-attempt recovery. Add actual client→HTTP→restricted SQL→Driver reply tests. No new schema migration/artwork/phone activation; mounted drawer and browser reload recovery remain open.
- 1.6 (2026-09-10): connect actual original issue reads to the staged G03 report/reopen callback and expose loading/waiting/instructions/failed-last-known state. Preserve encrypted reports and immediate navigation; no new approved artwork, Dispatch, schema or phone activation.
- 1.5 (2026-09-10): implement restricted Operations wait/escalate and actual original Driver reply query (ADR-T08). Isolated migration0008 preserves old decisions and narrow report access. No source UI/phone changes; physical decision branches and approved host recovery remain open.
- 1.4 (2026-09-10): ADR-Q04 adds actual original-report status/decision reads and receipt-bound native transport. No visual change. Supplied post-custody waiting/return copy is not an approved pre-pickup variant; ResolveIssue/host screens remain open. Current aggregate evidence is recorded separately from the previous disk-limited run.

- 1.3: implement approved G03 choices/evidence layout, mutable encrypted draft/camera candidates and explicit report submission through the existing native transport. Retakes/restart/latest-note/double-submit tests and Flutter captures are local evidence, not source-render parity, host recovery or phone activation.
- 1.2: ADR-S08 connects encrypted parcel selection/confirmation intent, original arrival and the existing execution/status queue to the shared view. Add current-lease controller and staged route binding; no invented recovery artwork, main/phone cutover or legacy writes.
- 1.1: add ADR-Q03 restricted pickup display query, generated wire, authenticated native lease and adapter for the existing view. Use actual parcel IDs/contents, preserve first execution authority, and expose the unpacked-order presentation gap. Do not switch the installed/legacy route.
- 1.0: port approved D03/D04 and its first-level drawer into the existing English route. Preserve installed data/transport and the Thai screen. Record source-render/font and downstream issue-page gaps explicitly.

## Reference and implementation

### Dispatch issue connection (ADR-Q05)

[Operations query](../../services/api/src/v23/operations-issue-query.ts) now discovers actual original ReportIssue records by city/service date under live issues.decide rights, returning report/evidence IDs, current instructions and bounded wait/escalate actions. Opt-in display=labels supplies current delivery reference, sole recipient name, address, pickup site and original reporting-driver name from the same restricted transaction. Missing/ambiguous/blank/overlong/archived values are null, not substitutions; no contact secret/channel, private note, Auth ID or asset URL is selected. The query and locked command share original-job eligibility predicates; changed work is non-actionable with all labels suppressed. Round-only waiting has no fabricated delivery. Absent display retains the earlier exact response, while a label-requesting browser rejects omission of the object. No new migration, role widening or domain write. Current labels are not original snapshots and cannot rebase a reply or enter the tab journal.

[Browser controller](../../apps/operations-web/src/v23/operations-pickup-issues.ts) connects that query to ResolveIssue/CommandStatus, with generated static validators, no runtime unsafe-eval compiler, bounded reads, same-scope/coalesced refresh, immutable displayed versions and exact-attempt retries. The new [recovery journal](../../apps/operations-web/src/v23/operations-issue-recovery.ts) optionally persists one editable original-version draft and frozen attempt in sessionStorage before sending. Fresh scoped authorization precedes hydration; there is no cached permission or automatic POST. Failed storage, corrupt records and competing writers block sending. Unknown attempts cannot be edited/discarded/replaced; failed settlement preserves the original attempt, while successful settlement persists committed-version floors and clears the finished draft. Stale draft scope requires explicit review, never a silent rebase.

[Controller tests](../../apps/operations-web/test/v23-operations-pickup-issues.test.ts), [reload tests](../../apps/operations-web/test/v23-operations-issue-recovery.test.ts) and the [actual Node/PostGIS round trip](../../services/api/test/v23/resolve-pickup-issue.postgis.ts) cover these boundaries, including response loss after real commit, controller recreation and exactly one POST. Browser Storage/Auth are labelled doubles; no real browser disk/reload acceptance is claimed. This is tab/login-local draft convenience, not encrypted Driver evidence, cross-tab/background sync or guaranteed tab-close/eviction recovery. No bearer, projection, grants or media URLs are journalled. The future host must supply stable verified login identity, rotate/erase it on Auth teardown, and use hasUnsavedWork for dirty-close review. Ordinary dispose preserves the record; dispose(true) and login-wide cleanup erase only owned new journals, not legacy work.

The controller now feeds [Phase39-derived React cards/detail/form](../../apps/operations-web/src/v23/pickup-issue-drawer.tsx) with source delivery-card/name/address/status/action hierarchy, scoped source colors, standalone/wide 400px detail surface, 24/28px detail insets, 448px action dialog, 40px actions and source typography/fallback stack. Only the bounded own-team wait/escalate addition was approved10 September. Names/address/site/reporting-driver come from authorized display fields; original query identifiers remain inspectable. Missing fields stay unavailable rather than demo names, invented Round numbers, ETA or map content. Exact instructions and separate internal reason stay editable until Send; pending results are frozen/status-first, source failures are last-known and closing offers Keep/Discard without sending. Native dialog traps focus, Escape opens draft review and closing restores row focus or filtered-list search. The same component is now mounted in the [Phase39 shell](W01-DISPATCH-BOARD-CHECKPOINT.md), using source rail/wide-detail/tablet breakpoints. General delivery-board/map data and secure evidence viewing remain integration work.

The [standalone route](../../apps/operations-web/app/pickup-issues/page.tsx) `/pickup-issues` and [board route](../../apps/operations-web/app/dispatch/page.tsx) `/dispatch` share the same default-off connection/configuration, not two reply implementations. Before build/restart, an isolated operator environment must set `ROUNDS_V23_OPERATIONS_UI=1`, `ROUNDS_V23_OPERATIONS_API_ORIGIN`, `ROUNDS_V23_OPERATIONS_PRINCIPAL_ID`, `ROUNDS_V23_OPERATIONS_TENANT_ID`, `ROUNDS_V23_OPERATIONS_CITY_ID`, `ROUNDS_V23_OPERATIONS_SERVICE_DATE`, and existing public Supabase Auth configuration. These are exact isolated v2.3 scope assertions, not authority or legacy-ID mappings; the API independently verifies identity/capability. No secret/service credential is sent as page configuration. `getUser` verifies the exact bearer before its subject/session lineage is used to bind an opaque tab login ID. Token renewal/reload retains that ID; new login rotates and narrowly clears departing journals. Auth events immediately dispose/hide old controller state; generation checks reject late verification and component keys include the complete scope. Failed erasure/storage blocks sending. The [login tests](../../apps/operations-web/test/v23-pickup-issue-view.test.ts) use explicit Auth/Storage doubles. Configured-provider verification, cross-tab logout while the host is absent and all production CSP behavior are NOT accepted by these tests.

User supplied four original Phase39 screenshots10 September at11:00:37,11:01:02,11:01:13 and11:01:20, including the recipient issue detail drawer. They are valid bounded reference evidence together with readable static HTML/CSS. The later action form is not shown. Original file-scheme browser navigation remains blocked; it was not rehosted or bypassed. User authorized continued implementation from this evidence; interactive source/form comparison and full-board pixel parity remain open. The source HTML is unchanged, the existing v45 root and installed phone are not switched, and the separate Driver pre-pickup waiting proposal remains unapproved.

Actual browser checks use [our component test host](../../tools/review_pickup_issue_ui.mjs), not the original HTML or a production board: synthetic issue/Auth/HTTP on loopback, actual React/native fetch/sessionStorage/static validators and a CSP without unsafe-eval. Current [detail](generated/pickup-issue-labels-detail.jpg) and [card](generated/pickup-issue-labels-card.jpg) screenshots are normal viewport captures at400×900 and400×800, including the synthetic-test banner. [Current browser evidence](generated/pickup-labels-browser-check.json) checks1366×900,1024×768,400×800,320×640 and400×300: no horizontal document overflow, 40px reply button inside each viewport. Initial1px overflow in the bordered synthetic wrapper was corrected, as was root border-box sizing. Final computed card values match the source's later queue overrides:16/12/12px padding,9px radius,16px/650 name,12px address,11px issue/action text and36px action row. Multiline reply→unknown/frozen controls→status recovery displays the exact instruction with heading focus. The fixture JSON counter page was blocked by the client; no alternate access was attempted and the final browser POST counter is not claimed read. Automated real-SQL/client tests independently assert one POST/decision. A clean reload after code changes was observed; dirty-draft browser reload remains unaccepted. Incorrectly scaled full-page/cropped API captures were replaced by normal viewport screenshots after checking settled DOM bounds. No original/source parity, configured browser→SQL→phone acceptance, deployment or push follows from this test host.

Previous bounded-form browser checkpoint: [form screenshot](generated/pickup-reply-form-1366.png) at1366×768, five larger viewports, dirty-close keep/reopen, heading/row focus and synthetic unknown-result recovery. The dirty reload attempt was cancelled by browser navigation handling (unchanged query timestamp), not passed. Native-fetch receiver, contradictory pending-draft copy and the production runtime TS import failure were fixed then. These earlier checks are historical; current labels/border geometry use the separate evidence above.

Baseline commit: `19d6c1e7dc570546026cfb41135fae74c6b4e23e`; current fingerprints and executed checks: [run evidence](generated/test-results.json).

[Approved D03/D04](../source/Rounds-Complete-Project-v2.3/ui/driver/screens/ROUNDS-D03-D04-PICKUP-CONFIRM-v6-10OF10.html), SHA256 `ae682dcd0c2f25d214261da2c939776f2b5180e57671f04ab5d956939d076b02`. Refresh02 is this screen's revision inside Refresh26. Source unchanged.

[Native view](../../apps/driver_harness/lib/src/ui/components/pickup_collection_view.dart) and [legacy route adapter](../../apps/driver_harness/lib/src/ui/pickup_confirmation_screen.dart) replace only the English surface. Source-sized 64px header, minimum 92px pale-blue progress region, full-width package rows, pinned 68px confirmation, and four-action inset drawer. Inline SVG paths are transcribed as native paths. The legacy adapter expands its old integer count into display pieces; that mapping is NOT v2.3 package authority. The new adapter below uses explicit package identities only. Legacy confirmation submits the entire original manifest, never selected partial quantities. Selected rows/checks are blue, ready confirmation green. No sample recipient or job IDs enter production code.

320/360 widths and short screens use source media-query insets/minimum heights. Enlarged text remains enabled; rows/controls grow rather than clipping. On the legacy port, back/dismiss/Escape retain current-route selection; changed round/version/content clears it and process persistence is absent. The separate v2.3 adapter below durably preserves checks and rejects changed scope/content without silently clearing them.

Confirmation still calls the actual legacy controller/API/outbox. Committed, sending, pending and failure remain separate. Inspection found `_queueAndSend` falsely returning pendingSync when unconfigured without writing an outbox record. It now rejects explicitly without a sent/saved claim; configured offline behavior is unchanged. The navigation test uses an explicitly labelled committed receipt instead of that fake result. Four legacy UI tests exposed the same incorrect expectation for location, missing-package, arrival and contact actions. They now assert an error without fabricated saved/pending state. The two-recorded-contact action-drawer test uses an explicit display fixture; real queue persistence remains separately tested.

Missing/wrong/damaged pass to the existing pickup issue form/command, not the dropoff damage command. Message Operations opens the existing first-stop thread. Downstream G03 evidence UI and multi-stop thread selection are NOT claimed as refreshed or fully source-matched. No new Thai layout, migration, shared database, installation, credentials, provider activation, deployment or push.

## Authorized v2.3 display connection

[DriverRound query](../../services/api/src/v23/driver-execution-query.ts) implements explicit view=pickup in the same restricted REPEATABLE READ transaction and assigned-job/device authorization as view=execution. It returns only current merchant/site, delivery reference, one unambiguous recipient display name, exact manifest lines and actual packages/contents. No contact secret/channel/private address, inferred parcel, readiness mutation or receipt. Names do not enter the unchanged execution scope digest; existing execution clients receive the same shape.

[Native parser](../../apps/driver_harness/lib/src/v23/execution_query.dart), [registered transport](../../apps/driver_harness/lib/src/v23/device_registration_client.dart), [current lease](../../apps/driver_harness/lib/src/v23/driver_storage_lifecycle.dart) and [Auth adapter](../../apps/driver_harness/lib/src/v23/driver_storage_auth.dart) return fresh display plus the FIRST encrypted execution context separately. Later labels/versions cannot rewrite an observation's original fence. Query supersession, account lock and response scope checks apply before data is exposed. ADR-S08 now preserves the first display/checklist in the encrypted collection record; this does not implement offline authentication/unlock.

[PickupPresentation](../../apps/driver_harness/lib/src/v23/pickup_presentation.dart) feeds the existing approved component. A real package ID becomes one row, regardless of the sum of its contents. Five items in two packages remain two packages. Package contents must match whole manifest lines exactly in integer ten-thousandths. Optional missing packaging stays packages=[] on the server; the parcel-only adapter returns PICKUP_PACKAGE_DISPLAY_UNAVAILABLE rather than inventing IDs or calling items packages. That unpacked-order UI state remains an explicit design gap, not a business requirement to prepackage every order.

Source/contracts/acceptance reconciled together in E03, OpenAPI and ADR-Q03, then generated TS/Dart closures and hashes refreshed. Baseline commit unchanged. Dependencies: registered authenticated own-team assignment, local ready/pre-arrival semantics from W01, authoritative manifest/contact/package rows, and existing first-context storage. Migration: additive query view only; no server/local schema change, old-wire change, file deletion, account import or dual-write. The new adapter is not yet selected by main/legacy route; no confirmation is committed by a display query or widget test callback.

ADR-Q04 [server reader](../../services/api/src/v23/pickup-issue-query.ts) and [native current lease](../../apps/driver_harness/lib/src/v23/driver_storage_lifecycle.dart) fetch original reported issue status and actual instruction records, without authorizing pickup/continuation. The native selector comes from a validated committed request/receipt and does not refresh original versions. [Reader](../../services/api/test/v23/pickup-issue-query.postgis.ts) and [native](../../apps/driver_harness/test/v23_pickup_issue_query_test.dart) tests distinguish open/no reply, informational decision, access/chain failure and late/account-superseded reads. Reader-only tests seed decision/corruption fixtures; [ADR-T08 tests](../../services/api/test/v23/resolve-pickup-issue.postgis.ts) now produce actual wait/escalate decisions through the Operations handler. Other actions and approved pickup waiting/recovery UI remain open. No source markup was edited or phone route activated.

## Captures and verification

### Original-arrival confirmation connection

[Encrypted collection](../../apps/driver_harness/lib/src/v23/pickup_collection_store.dart) uses existing work_cache/pickup_collection_v1, one record per principal/original assignment revision/pickup. It preserves actual package IDs/contents, display, original context, actual arrival and stable observation ID; explicit checkbox values are durable. All parcels must be checked. Complete quantities come from manifests, never the checkbox count. Missing packages remain the explicit unpackaged-design gap. No schema migration, old-data conversion, source UI mutation or permanent dual writer.

[Controller](../../apps/driver_harness/lib/src/v23/pickup_collection_controller.dart) commits confirmation intent/time before recording the observation, then invokes the existing materializer/synchronizer. Original arrival must actually exist and be committed before pickup can send. Never synthesize arrival from a query. Crash boundaries and concurrent controllers reuse the same ID/time/bytes; unknown requests recover by original status with retry-after. A rejected/review/waiting/local result is not committed. Reassignment preserves original records; needs_review is not the still-unimplemented incident acknowledgment.

[Staged route](../../apps/driver_harness/lib/src/v23/pickup_collection_route.dart) renders the existing D03/D04 component and wires real checkbox/confirmation actions. Only committed receipt calls onCommitted; other outcomes delegate to the host's recovery destination. Back/problem remain host callbacks. It intentionally contains no new waiting/review page and is not selected by main or the installed legacy screen. Host Auth/mapped migration, actual arrival route, approved G03 recovery and absent-state design decisions remain activation dependencies.

[27 connection tests](../../apps/driver_harness/test/v23_pickup_collection_test.dart) cover actual SQLCipher selection/reopen, four confirmation commit-boundary interruptions, exact full quantities/versions, repeated taps/controllers, pending original arrival, lost response/retry-after, authoritative rejection, invalidation before/after send, late sign-out, source-shared widget actions, unpacked refusal and no fabricated timestamps. Auth/HTTP/vault are doubles; DB/files/queue/Flutter are actual code. Executed v1.2 checkpoint at 2026-09-09 06:52:43 UTC: all 20 groups PASS, including 487 full Flutter tests (304 native subset tests are included, not additional), 201 API/foundation and 285 full PostGIS tests. Static analysis, separate SQLCipher process-kill checks, Android bridge ordering and ARM64 debug build also PASS. Exact commands/fingerprints/logs are in generated/test-results.json. A configured phone-server journey, source diff and device power-loss test are not claimed.

[Geometry/capture tests](../../apps/driver_harness/test/pickup_confirmation_geometry_test.dart) and [interaction tests](../../apps/driver_harness/test/pickup_confirmation_test.dart). Standard content viewport 393×852, DPR 1, text scale 1, English, source-matching synthetic package fixture. OS/status chrome and debug banner omitted. Also 320×720, 360×640 and 320×852 at 1.6 text scale.

Generated actual Flutter images, NOT source screenshots:

- [Initial](../../apps/driver_harness/build/refresh26-pickup/pickup-initial.png)
- [All six selected](../../apps/driver_harness/build/refresh26-pickup/pickup-selected.png)
- [Action drawer](../../apps/driver_harness/build/refresh26-pickup/pickup-drawer.png)

Previous v1.0 checkpoint, 2026-09-09: 20 groups passed, including 431 full Flutter tests, 201 API/foundation tests and 276 full-schema PostGIS tests. Those counts are historical, not evidence for subsequent edits. Current source/code fingerprints, commands, PASS/FAIL and logs are in [run evidence](generated/test-results.json).

Executed v1.1 checkpoint at 2026-09-09 06:32:07 UTC: all 20 groups PASS, including 460 full Flutter tests (277 native-subset tests are included, not additional), 201 API/foundation tests and 285 full-schema PostGIS tests. Static analysis, SQLCipher process-kill recovery, Android bridge ordering and ARM64 debug build also PASS. The 29 new native tests and nine new database subcases are contained in those totals. No source-render, configured provider, phone journey or release acceptance follows from these local results.

New [real PostGIS query tests](../../services/api/test/v23/driver-execution.postgis.ts) cover two parcels/five items, exact decimal contents, absent/ambiguous/blank/long labels, missing/excess contents, no-package truth, original execution compatibility and both views' identity/job denials. [Native query/presentation tests](../../apps/driver_harness/test/v23_pickup_query_test.dart) cover schema/ID/content rejection before cache, first-context preservation across refresh/reopen, late responses/sign-out, mixed-view supersession, no-package refusal and real rendering/selection of the shared widget. HTTP/Auth/vault are labelled doubles; encrypted storage and Flutter rendering are real. They are not a configured phone-to-server journey.

Set ROUNDS_TEST_FLUTTER_SDK to the existing SDK path for the local font loader and ROUNDS_PICKUP_CAPTURE_DIR to an output directory for capture. Explicit Android Roboto fallback is provisional, not approved Arial/Helvetica equivalence. No proprietary font copied/bundled. The previous pre-v2.3 golden stays historical; its tolerant comparator no longer certifies D03.

SOURCE_RENDER / SOURCE_DIFF / FONT_APPROVAL / PHONE: NOT_RUN. Prior browser file-URL restriction respected; no rehosting or alternate browser route. This is staged implementation from static CSS and actual Flutter captures, not visual acceptance. Neither geometry checks nor these images complete the whole screen journey. Per-state code: default/selected/first-level drawer implemented; legacy transport unchanged. V2.3 durable checks and confirmation/status orchestration exist, but waiting/retained/G03 visual destinations and main/phone routing remain open.

## G03 editable issue form and explicit report

[Approved G03](../source/Rounds-Complete-Project-v2.3/ui/driver/screens/ROUNDS-G03-PACKAGE-PROBLEM-v3-10OF10.html), SHA256 `317a140e1d5c1da74bd6eccc2a4856bb0ba7386b36617d5359ec3ae9d6ac0407`, unchanged. [PackageProblemView](../../apps/driver_harness/lib/src/ui/components/package_problem_view.dart) transcribes source colors, inline icons and layout: 64px header, pale-blue/orange heading, minimum88px package/choice rows, 212px photo area, optional90px details and pinned64px primary with contact row. Narrow/short/enlarged-text cases scroll; one CSS pixel remains one logical unit. Source post-handoff custody/wait instructions are not valid pickup instructions and are not fabricated. Unpacked-package display remains the known gap.

[Encrypted form](../../apps/driver_harness/lib/src/v23/pickup_issue_form_store.dart), [controller](../../apps/driver_harness/lib/src/v23/pickup_issue_form_controller.dart) and [staged route](../../apps/driver_harness/lib/src/v23/pickup_issue_form_route.dart) use real authorized pickup data, immutable original account/assignment and the shared camera guard/uploader. Choices are Damaged, Missing and Wrong package. Damaged/wrong require a saved candidate; missing requires none. Notes stay editable until explicit Request instructions. Reason changes clear selection but not old images. Cancelled retakes retain the previous selection; interrupted capture recovery requires exact saved provenance. Submission waits for draft writes, freezes latest exact note/selected capture/time, then uses actual reserve/PUT/verify/ReportIssue and status. Empty optional details are omitted; no fabricated arrival, receipt, custody or decision. Reopening a frozen form goes to host recovery/report status instead of a dead-end locked form.

No local/server schema change, legacy import, dual-write, file deletion or old queue rewrite. New form/camera entities live in existing encrypted work_cache; candidates attach only through a private validated binding. Existing immutable direct-report camera callers remain compatible. The route is staged, not selected by main. Back/more/contact and the destination artwork remain explicit host callbacks. The report-success/reopen callback now starts the actual receipt-bound issue read and immediately hands the same controller to the host in loading state; it does not wait for network completion to navigate. The host retains the controller, listens for changes and uses its explicit read-only refresh. Endpoint binding is implemented here; destination artwork and main routing are not.

[PickupIssueRecovery](../../apps/driver_harness/lib/src/v23/pickup_issue_recovery.dart) separates a committed report from an Operations reply: loading, successful empty decisions/waiting, actual instructions and unavailable reads. Exact instruction text/as_of remains available; previous text is last-known while refreshing or failed, never silently current. Coalesced taps send one GET, older issue versions cannot overwrite newer instructions, and a pinned original Auth lease discards late results after sign-out/reauthentication/disposal. A reply never supplies canContinue/ready/complete. Reopen reads the original encrypted receipt and starts without a durable cached reply; no offline authority is introduced. Test fixtures use real encrypted files with labelled HTTP/Auth/vault doubles.

Reference gap confirmed this increment: G03 waiting/decision wording says keep goods in custody and continue to remaining stops/return later. It does not describe an uncollected pickup. A minimal reuse proposal (existing waiting layout, “Waiting for Operations,” actual reply, Refresh/Message Operations, no custody/continue wording) was sent for user review; it is not approved or implemented by silence. Original UI bytes remain unchanged. The prior source-render policy gate remains respected.

Local tests: [19 store cases](../../apps/driver_harness/test/v23_pickup_issue_form_test.dart), [23 controller/route cases](../../apps/driver_harness/test/v23_pickup_issue_form_controller_test.dart) and [7 widget cases](../../apps/driver_harness/test/v23_package_problem_view_test.dart). Real SQLCipher/AES-GCM/files and Flutter; camera/Auth/HTTP/vault are labelled doubles. Cover latest notes, no-photo reports, retakes, reason changes, selected-capture tampering, unresolved-camera/account changes, file-save/submission interruptions, lost report responses, repeated taps, failed writes and actual choice-to-submit callbacks. The15 new recovery cases cover original reads, waiting/exact superseding instructions, unavailable/last-known states, coalescing, version regression, scheduled/pending/completed reads across Auth changes, disposal, immediate reopened-route handoff and controller replacement. Exact current results and fingerprints belong to generated/test-results.json.

Actual Flutter regression images at393×852, DPR1, English, source-like synthetic content:

- [Choices](../../apps/driver_harness/test/goldens/v23/g03-choices.png)
- [Damaged evidence form](../../apps/driver_harness/test/goldens/v23/g03-damaged.png)
- [Missing details form](../../apps/driver_harness/test/goldens/v23/g03-missing.png)

These images are generated from the new component, NOT original HTML screenshots or independent visual acceptance. SOURCE_RENDER / SOURCE_DIFF / FONT_APPROVAL / PHONE remain NOT_RUN. No bypass of the existing browser file-URL restriction. The component still uses provisional SDK Roboto, not approved Arial equivalence. No APK rebuild/install, configured provider, shared migration, deployment or push in this increment.

### Current verification outcome

Dispatch-shell increment, recorded 10 September 06:06:59 UTC: **all 23 executed groups PASS**: **122/122 Operations tests**, **454/454 full-schema PostGIS**, **201/201 API/foundation** and **618/618 full Flutter** (435 native-subset tests included, not additional). Nine new Operations checks cover bounded composition, disabled unsupported controls, stale/Auth states, local search/draft preservation, nullable labels, configuration and source geometry. The earlier label/compatibility and 57 focused decision/query checks remain covered, including real client→HTTP→restricted SQL→Driver reply after response loss AND controller recreation, producing one POST/decision. Static validators/contracts, typechecks, production Operations build, native analysis, actual SQLCipher process-kill and Android bridge checks pass. The initial new privacy guard exposed three unreachable closed-state comparisons at typecheck; those were corrected before the final aggregate. No automated failures remain. [Exact commands/logs/fingerprints](generated/test-results.json).

Separate [current board-browser evidence](generated/dispatch-pickup-board-browser-check.json) verifies nine viewport geometries, pinned reply action, local search/no-match/focus, kept drafts across tablet switching and frozen unknown-result/status recovery. [Earlier label/card checks](generated/pickup-labels-browser-check.json) cover the standalone component. HTTP/Auth/report data are labelled synthetic; no real driver receives a reply. The fixture POST counter page was blocked and was not read or bypassed; automated SQL/client assertions remain separate. [Earlier form checks](generated/pickup-reply-browser-check.json) cover dirty-close keep/reopen and compiled default-off state. Actual dirty-browser reload was cancelled, not passed. Full Phase39 board/source parity, configured Auth/provider/SQL host, secure evidence viewing and phone acceptance remain open. The user's original screenshots and readable static CSS support the bounded port but do not prove pixel parity. All116 protected UI/reference files are unchanged. No shared migration, phone install, deployment or push. Next implement the general authorized Board/workspace/map projection; the pickup workflow is already mounted in the bounded source-derived shell.

Previous label/card checkpoint,10 September05:50:12 UTC:23 groups passed with113 Operations,454 PostGIS,201 API/foundation and618 Flutter tests. It preceded the shell/search/responsive integration above.

Previous drawer/host checkpoint,10 September04:47:38 UTC:23 groups passed with108 Operations,446 PostGIS,201 API/foundation and618 Flutter tests. It preceded the current display fields and final-card-style check.

Previous draft/reload checkpoint, 10 September 03:44:17 UTC: 22 groups passed with 96 Operations, 446 PostGIS, 201 API/foundation and 618 Flutter tests. It preceded the mounted component and production-build checks and is historical, not evidence for later edits.

Previous ADR-Q05 discovery checkpoint,10 September03:21:11 UTC:22groups passed with67 Operations,445 PostGIS,201 API/foundation and618 Flutter tests. It preceded the draft/reload implementation and remains historical, not evidence for these later edits.

Previous native-recovery checkpoint,10 September02:59:44 UTC:19groups passed, with618 Flutter,437 PostGIS and201 API/foundation tests. That result covered staged G03 report/reopen-to-reply binding before ADR-Q05 Operations discovery/browser transport. It did not install the app or accept approved waiting artwork, Dispatch UI, configured providers or source/font parity. Current evidence above supersedes these historical counts.

Previous decision-writer checkpoint, 10 September, 02:26:29 UTC: all19 executed groups passed, with437 PostGIS (40 new decision/upgrade checks),201 API/foundation and603 full Flutter tests. ADR-T08 exercised actual restricted ReportIssue→Operations wait/escalate→original Driver query, replay/races/rollback/privacy and an old-schema upgrade. These historical results preceded the native binding above; current logs/fingerprints supersede them.

Previous reader-only checkpoint, 10 September, 01:48:26 UTC: all19 executed groups passed, including603 full Flutter,397 full-schema PostGIS and201 API/foundation tests. Its reader tests used explicitly seeded Operations decisions; it did not yet implement ResolveIssue. These are historical counts, not the current decision-writer evidence above. Neither checkpoint claims configured provider, phone journey or visual acceptance.

Previous G03 checkpoint (9 September): focused run **34/34 PASS** in [G03 log](generated/run-g03-focused-tests.txt), using `flutter test --no-pub --concurrency 1` for the three G03 test files and ROUNDS_TEST_FLUTTER_SDK set to the repository's sibling flutter-sdk. Scoped native analysis: no issues. The checkpoint's source/contracts/typechecks/API+PostgreSQL foundation and actual SQLCipher process-kill checks passed. [Android bridge recheck](generated/run-g03-android-barrier-recheck.txt) passed after explicitly supplying the existing work/jdk21 Java binary, Android36 jar and SDK-matching Gradle Flutter embedding jar; the earlier aggregate attempt lacked those environment settings.

The 9 September aggregate failed when full PostGIS initialization and native compilation exhausted disk space; its initial bridge invocation also lacked runtime settings. This is historical, not the status of the 10 September run. No source, app databases, phone files or photos were deleted. Current source/code fingerprints, complete logs and individual PASS/FAIL results are in [test-results](generated/test-results.json). APK/phone/provider/source visual acceptance remains NOT_RUN regardless of automated results.

## Next integration boundary

The staged Driver report/reopen recovery, Operations reply controller/journal, authorized labels and source-derived React drawer are implemented, including placement in the [bounded Phase39 shell](W01-DISPATCH-BOARD-CHECKPOINT.md) at default-off `/dispatch`. Next implement the general authorized Board/workspace/map projection without legacy-ID substitution and verify a configured isolated Auth/SQL host. Complete actual browser reload/logout/CSP tests and original form comparison before activation. Driver pre-pickup waiting remains a separate proposal. Resolve absent unpacked/review states using approved components; host arrival/route flow still depends on compatibility gates. Other ResolveIssue actions remain disabled. Configured storage, non-destructive mapped upgrade, source/font comparison and phone activation remain separate. New Thai stays deferred.
