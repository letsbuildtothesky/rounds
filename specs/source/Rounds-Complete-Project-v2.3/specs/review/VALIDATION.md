# Validation actually performed — V2.3

8 September 2026. This report records specification/source checks, not a production certificate.

## Executed

- Read-only package validator: JSON/schema/reference resolution; command/catalog/error parity; finite synthetic examples; policy discriminants; unit selectors and assignment fences; state reachability and event/consumer identities; SQL syntax and table/invariant inventory; SQLite clean-schema execution; acceptance ownership and local document links.
- Six executable reference examples: own-team idempotency model, freelance graph closure, complete-order readiness and split prohibition, offline identity binding, settlement arithmetic and independent receipt readiness.
- Published fixture UUID derivation checked against every fixture alias.

The exact stdout from each validator is preserved in VALIDATION-OUTPUT.txt and JOURNEY-EXAMPLES-OUTPUT.txt. These outputs are generated by the actual scripts. The package validator is checked for no file mutations during the final run.

The 352 positive contract instances are synthetic schema fixtures. The 197 acceptance entries are written requirements and remain unexecuted against an application. The six executable reference examples are small model/schema/graph checks, not six end-to-end application tests. SQL parsing does not typecheck or apply the schema.

## Not executed

- PostgreSQL/PostGIS schema application, RLS/column privileges, trigger behavior, competing transactions, rollback, GPS purge/hold races or actual Supabase realtime revocation. No PostgreSQL server was available in this environment.
- HTTP command handlers, worker consumers, provider integrations, generated TypeScript/Dart client compilation or production migration.
- Native phone offline replay, encrypted storage, crash recovery, navigation/battery field harness or device GPS behavior.
- Browser visual parity, driver interaction coverage or iPad behavior. Approved UI files are preserved as design references.

A prior reviewer's V2.1 database results are provenance only. They do not certify this changed schema. See REVIEWER-GUIDE for the next independent checks and database/MIGRATION-BOUNDARY.md for the exact fresh-schema application order.

## V2.3 scope validation

Validators rerun against V2.3. J03 checks complete-order readiness, null partial-approval compatibility field, empty residual outputs and one-unit/no-child SQL definitions. Reserved ApprovePartialPickup shapes remain documented for compatibility; a synthetic shape fixture does not imply launch permission. Actual restricted-role SQL execution and API tests remain pending.
